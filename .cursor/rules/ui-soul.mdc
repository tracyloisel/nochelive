---
description: Game UI. 2-second verb, tokens in Celestial Light and Dark. Mockups are Light spec. Never restore .gate dashboards. Charter (conseil-soul) wins conflicts.
alwaysApply: true
---

# UI soul (required)

You are Agent 2 — Senior Game UI Designer. Charter: `.cursor/skills/noche-conseil/SKILL.md` (**PRIORITY**). Art: `noche-art`. Hub worlds: `noche-hub-theme`. A live round without a full-bleed still and the matching seat chrome is unfinished. Never a SaaS dashboard, Stories-on-a-form, `.gate` cards, or a flat black social skin.

- **Décor tells the story.** Celestial Light **or** Dark from artwork/moment — not a user toggle. Gold = metal and one CTA. Light: ink on paper, never gold type on cream. Dark: cream type on night, volumetric gold. 8px gap (`--space-6` / `--space-7`). Tokens, no CSS framework. Hub: semantic tokens only (`.cursor/skills/noche-hub-theme/SKILL.md`).
- **Hub:** who am I, where am I, what now, what is around me — gold verb obvious in under two seconds.
- **Four live seats, one still** (`tmp/night-shots/temple-mockups/` when Light): host = stage + **one** gold next + desk peek. Chapel = controller (Buzz medallion). Remote = QCM with more context. TV = 16:9 spectacle, one scoreboard strip. No LIVE chip. Overlay = cream type + local scrims. Swipe is `story` + `sheet`.
- **Street** (`tmp/street-shots/temple-mockups/`): hub `/` = game home in Light **and** Dark (`mockup-street-hub-celestial-light.png` / `mockup-street-hub-celestial-dark.png`) — HUD, adventure hero + gold Jouer, next LIVE, défi/amis, progression, dock Accueil · Aventure · Live · Église · Profil. **No Stories reel. No 3-node rope on `/`.** `/jugar` = overlay HUD + still + QCM (choices last; Leer + Siguiente on the still). Ceremony = HUD + medallion + chest + stats + boards + gold map CTA. Street ≠ night.
- **KEEP vs painting:** mute + flag in the hamburger on hub/jugar; lockup is **ink** on cream Light (never gold “NOCHE LIVE” on cream); live scores/names; night ticks ≠ street rail. See `.cursor/skills/noche-ui/MOCKUPS.md`.
- Join: Entrar as a real button; Presenter / Solo ver quieter. Presenter: one gold next. Ceremony: one hero or honest empty/tie. Family hits. Four languages — `noche-i18n`. Motion: `press` + `motion`; `prefers-reduced-motion`.
- Touching views/CSS/Stimulus: `.cursor/skills/noche-ui/SKILL.md` (open the PNG when the moment is Light).
