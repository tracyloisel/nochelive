<section id="<%= hall_id %>" class="home-paper hall-paper<%= " #{extra_class}" if extra_class.present? %>">
  <header class="home-brand">
    <%= render "shared/street_hub_lockup", kicker: kicker %>
  </header>

  <% if sheet %>
    <article class="hall-sheet<%= " #{sheet_class}" if sheet_class.present? %>">
      <span class="hall-sheet-apex" aria-hidden="true"><%= picto("star4") %></span>
      <%= body %>
    </article>
  <% else %>
    <%= body %>
  <% end %>
</section>
