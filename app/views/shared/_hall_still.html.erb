<% src = local_assigns[:src] %>
<% if src.present? %>
  <figure class="hall-still">
    <%= noche_media_picture(src, alt: "", class_name: "night-poster", loading: local_assigns.fetch(:loading, "eager")) %>
  </figure>
<% end %>
