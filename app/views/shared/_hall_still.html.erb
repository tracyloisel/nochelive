<% src = local_assigns[:src] %>
<% if src.present? %>
  <figure class="hall-still">
    <img class="night-poster" src="<%= src %>" alt="" loading="<%= local_assigns.fetch(:loading, "eager") %>" decoding="async">
  </figure>
<% end %>
