<% person = current_street_person %>
<% ward = current_ward %>
<% player = local_assigns.fetch(:player) %>
<% profile_path = person ? player_profile_path(person) : street_profile_path(fresh: 1) %>
<% ward_path = ward ? ward_profile_path(ward.code) : search_path %>
<% circle_ward = person&.ward %>
<% circle_path = scripture_circle_path if circle_ward&.scripture_circle_readable? %>

<nav class="home-menu-nav home-menu-nav-hub" aria-label="<%= t("chrome.menu") %>">
  <%= link_to profile_path,
        class: [ "home-menu-me", "hub-menu-profile", ("is-guest" unless person) ],
        aria: { label: person ? t("street.face_label") : t("street.pick_profile") } do %>
    <span class="home-menu-face" aria-hidden="true">
      <% if person && (src = avatar_mark(person)) %>
        <img src="<%= src %>" alt="" class="avatar-img" width="44" height="44">
      <% elsif person %>
        <span class="avatar-fallback"><%= person.given_name.to_s.first %></span>
      <% else %>
        <span class="avatar-fallback"><%= picto("profile-spark") %></span>
      <% end %>
    </span>
    <span class="home-menu-me-copy">
      <strong><%= person&.given_name || t("hub.guest_invite") %></strong>
      <small>
        <% if person %>
          <%= t("hub_menu.player_meta", rank: rank_name(player.rank_key), crowns: number_with_delimiter(player.crowns.to_i)) %>
        <% else %>
          <%= t("hub_menu.guest_meta") %>
        <% end %>
      </small>
    </span>
  <% end %>

  <section class="home-menu-block" aria-labelledby="hub_menu_play">
    <p id="hub_menu_play" class="home-menu-kicker"><%= t("hub_menu.play") %></p>
    <div class="home-menu-list">
      <%= link_to street_map_path, class: "home-menu-row home-menu-adventure" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("compass") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("hub_menu.adventure") %></strong>
          <small><%= t("hub_menu.adventure_hint") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <%= link_to street_challenges_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("duel-books") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("hub_menu.challenges") %></strong>
          <small><%= t("hub_menu.challenges_hint") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <%= link_to street_leaderboard_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("trophy") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("hub_menu.leaderboard") %></strong>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
    </div>
  </section>

  <section class="home-menu-block" aria-labelledby="hub_menu_scriptures">
    <p id="hub_menu_scriptures" class="home-menu-kicker"><%= t("hub_menu.scriptures") %></p>
    <div class="home-menu-list">
      <%= link_to scripture_library_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("book") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("scripture_library.title") %></strong>
          <small><%= t("scripture_library.menu_hint") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <% if circle_path %>
        <%= link_to circle_path, class: "home-menu-row" do %>
          <span class="home-menu-ico" aria-hidden="true"><%= picto("conversation") %></span>
          <span class="home-menu-row-copy">
            <strong><%= t("scripture_circle.hub_link") %></strong>
            <small><%= t("scripture_circle.ward_scope", ward: circle_ward.name) %></small>
          </span>
          <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
        <% end %>
      <% end %>
      <%= link_to ward_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("church-temple") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("hub_menu.my_ward") %></strong>
          <small><%= ward&.city.presence || ward&.name || t("hub_menu.choose_ward") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <% if ward %>
        <%= link_to search_path(cambiar: 1), class: "home-menu-row" do %>
          <span class="home-menu-ico" aria-hidden="true"><%= picto("search") %></span>
          <span class="home-menu-row-copy">
            <strong><%= t("hub_menu.change_ward") %></strong>
            <small><%= t("hub_menu.change_ward_hint") %></small>
          </span>
          <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
        <% end %>
      <% end %>
      <% if Notifications::Feature.enabled? %>
        <%= link_to notification_settings_path, class: "home-menu-row" do %>
          <span class="home-menu-ico" aria-hidden="true"><%= picto("bell") %></span>
          <span class="home-menu-row-copy">
            <strong><%= t("notifications.settings.menu") %></strong>
            <small><%= t("hub_menu.notifications_hint") %></small>
          </span>
          <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
        <% end %>
      <% end %>
    </div>
  </section>

  <section class="home-menu-block" aria-labelledby="hub_menu_settings">
    <p id="hub_menu_settings" class="home-menu-kicker"><%= t("hub_menu.settings") %></p>
    <div class="home-menu-list">
      <button type="button"
              class="home-menu-row mute"
              data-controller="menu-sound"
              data-action="menu-sound#toggle"
              aria-pressed="false"
              aria-label="<%= t("chrome.sound_on") %>"
              data-sound-on="<%= t("chrome.sound_on") %>"
              data-sound-off="<%= t("chrome.sound_off") %>">
        <span class="home-menu-ico mute-ico" aria-hidden="true">
          <%= picto("sound") %>
          <%= picto("sound-off") %>
        </span>
        <span class="home-menu-row-copy">
          <strong><%= t("chrome.sound") %></strong>
          <small class="word"><%= t("chrome.sound_on") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      </button>
      <%= render "shared/lang_switch", drawer: true %>
      <button type="button"
              class="home-menu-row pwa-install-action"
              data-pwa-install-target="action"
              data-action="pwa-install#install"
              hidden>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("sparkle") %></span>
        <span class="home-menu-row-copy">
          <strong><%= t("pwa.install") %></strong>
          <small><%= t("hub_menu.install_hint") %></small>
        </span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      </button>
    </div>
  </section>

  <section class="home-menu-block hub-menu-information" aria-labelledby="hub_menu_information">
    <p id="hub_menu_information" class="home-menu-kicker"><%= t("hub_menu.information") %></p>
    <div class="home-menu-list">
      <%= link_to about_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("info") %></span>
        <span class="home-menu-row-copy"><strong><%= t("hub_menu.about_us") %></strong></span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <%= link_to legal_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("scroll") %></span>
        <span class="home-menu-row-copy"><strong><%= t("hub_menu.legal") %></strong></span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
      <%= link_to privacy_path, class: "home-menu-row" do %>
        <span class="home-menu-ico" aria-hidden="true"><%= picto("lock") %></span>
        <span class="home-menu-row-copy"><strong><%= t("hub_menu.privacy") %></strong></span>
        <span class="home-menu-row-caret" aria-hidden="true"><%= picto("arrow") %></span>
      <% end %>
    </div>
  </section>

</nav>
