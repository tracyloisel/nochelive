<details class="home-menu" <%= "open" if local_assigns[:open] %>>
  <summary class="home-menu-btn quiet-link" aria-label="<%= t("chrome.menu") %>">
    <%= picto(local_assigns.fetch(:icon, "gear")) %>
  </summary>
  <div class="home-menu-panel">
    <%= body %>
  </div>
</details>
