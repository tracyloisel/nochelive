<% content_for :title, "Noche Live" %>
<% content_for :body_class, "is-kid is-street-hub is-game-hub-page" %>
<% screen = @screen %>
<% art = screen.backdrop %>
<% content_for :head do %>
  <link rel="preload" as="image" href="<%= art.src %>">
<% end %>
<section id="street_world"
         class="street-world is-game-hub<%= " is-returning-from-quiz" if params[:rank_up].present? %>"
         data-controller="street-share hub-scroll"
         data-street-share-copied-value="<%= t("street.share_copied") %>"
         data-hub-theme="<%= art.theme.mode %>"
         data-hub-atmosphere="<%= art.theme.atmosphere %>"
         data-hub-accent="<%= art.theme.accent %>"
         <% if params[:rank_up].present? %>
         data-stage-sfx-value="level_up"
         data-stage-sfx-token-value="hub:rank_up:<%= current_street_person&.id || "guest" %>"
         data-stage-fx-value="level"
         <% end %>
         style="--hub-art: url('<%= art.src %>'); view-transition-name: street-world">
  <%= chrome_menu(
        hud: true,
        bar: Huds::Present.from_screen(screen: screen, rank_up: params[:rank_up].present?)
      ) { site_menu } %>

  <div class="street-hub-feed">
    <%= render "street_hub/hero", screen: screen %>
    <%= render "street_hub/study", week: @study_week %>
    <%= render "street_hub/live", live: screen.live %>

    <div class="hub-panel-row">
      <%= render "street_hub/challenge", challenge: screen.challenge, screen: @challenge %>
      <%= render "street_hub/online", rows: screen.online, count: screen.online_count %>
      <%= render "street_hub/progress", progress: screen.progress %>
      <%= render "street_hub/community", community: screen.community, pulse: @pulse %>
    </div>
  </div>

  <%= render Navigation::DockComponent.new(active: :home, live: screen.live) %>
</section>
