<section class="circle-empty-state" aria-labelledby="circle-empty-title">
  <span aria-hidden="true"><%= picto("conversation") %></span>
  <div>
    <h2 id="circle-empty-title"><%= local_assigns.fetch(:title) %></h2>
    <p><%= local_assigns.fetch(:body) %></p>
  </div>
</section>
